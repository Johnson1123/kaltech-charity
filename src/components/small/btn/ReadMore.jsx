import React from "react";

function ReadMore({ text, to }) {
  return (
    <button className="readMore__btn" onClick={() => {}}>
      <span>{text}</span>
      <span></span>
    </button>
  );
}

export default ReadMore;
